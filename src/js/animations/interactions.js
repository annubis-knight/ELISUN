// Interactions utilisateur (hover, click, etc.)
export default class Interactions {
  constructor() {
    this.init();
  }

  init() {
    console.log('Interactions initialized');
    // Les interactions seront développées en Phase 4
  }
}